# Prisma 全栈 API 踩坑总结

## 📋 目录

1. [Prisma 7.x 语法变更导致 migrate 报错](#坑1-prisma-7x-语法变更导致-migrate-报错)
2. [PrismaClient 导入路径和实例化方式变更](#坑2-prismaclient-导入路径和实例化方式变更)
3. [ESM 模式下的扩展名问题](#坑3-esm-模式下的扩展名问题)
4. [ts-node 不支持 ESM，需要使用 tsx](#坑4-ts-node-不支持-esm需要使用-tsx)
5. [外键约束错误](#坑5-外键约束错误)
6. [MySQL 自增 ID 不从 1 开始](#坑6-mysql-自增-id-不从-1-开始)
7. [package.json 依赖拼写错误](#坑7-packagejson-依赖拼写错误)

---

## 坑1：Prisma 7.x 语法变更导致 migrate 报错

### 问题描述

执行 `npx prisma migrate dev --name init_user_post` 时出现以下错误：

```
error: Field declarations don't require a ':'
error: This line is not a valid field or attribute definition
error: The datasource property 'url' is no longer supported in schema files
```

### 问题原因

项目使用了 **Prisma 7.x** 版本，但 `schema.prisma` 文件仍然使用 Prisma 6.x 的旧语法。

### Prisma 7.x 重大变更汇总

| 变更项 | Prisma 6.x | Prisma 7.x |
|--------|------------|------------|
| `datasource.url` | 在 schema.prisma 中定义 | 移至 prisma.config.ts |
| `generator provider` | `prisma-client-js` | `prisma-client` |
| `generator output` | 可选（默认 node_modules） | **必填**，需指定路径 |
| `@relation` 格式 | 支持多行 | 仅支持单行 |
| 环境变量加载 | 自动加载 `.env` | 需手动引入 `dotenv` |

### 修复方案

**修改 schema.prisma**：

```prisma
// 1. 移除 datasource 中的 url 属性
datasource db {
  provider = "mysql"
}

// 2. 更新 generator client 配置
generator client {
  provider = "prisma-client"
  output   = "./generated/prisma"
}

// 3. @relation 改为单行格式
user      User      @relation(fields: [userId], references: [id], onDelete: Cascade)
```

**配置 prisma.config.ts**：

```typescript
import "dotenv/config";
import { defineConfig } from "prisma/config";

export default defineConfig({
  schema: "prisma/schema.prisma",
  migrations: {
    path: "prisma/migrations",
  },
  datasource: {
    url: process.env["DATABASE_URL"],
  },
});
```

**安装必要依赖**：

```bash
npm install --save-dev dotenv
npm install @prisma/client @prisma/adapter-mariadb
```

---

## 坑2：PrismaClient 导入路径和实例化方式变更

### 问题描述

原来的 PrismaClient 导入方式在 Prisma 7.x 中不再适用：

```typescript
// ❌ Prisma 6.x 写法，7.x 不适用
import { PrismaClient } from '@prisma/client'
const prisma = new PrismaClient()
```

### 问题原因

Prisma 7.x 将 PrismaClient 的生成位置从 `node_modules/@prisma/client` 移到了用户指定的 `output` 目录，并且需要使用驱动适配器（Driver Adapter）。

### 修复方案

```typescript
import 'dotenv/config'
import { PrismaClient } from '../prisma/generated/prisma/client.js'
import { PrismaMariaDb } from '@prisma/adapter-mariadb'

// 注意：直接传字符串，不是 { connectionString: ... } 对象
const adapter = new PrismaMariaDb(process.env.DATABASE_URL!)

const prisma = new PrismaClient({ adapter })

export default prisma
```

---

## 坑3：ESM 模式下的扩展名问题

### 问题描述

```typescript
// ❌ 错误：ESM 不识别 .ts 扩展名
import prisma from './prisma.ts'

// ❌ 错误：ESM 不允许省略扩展名
import prisma from './prisma'
```

### 问题原因

项目在 `package.json` 中设置了 `"type": "module"` 后，Node.js 进入 ESM 模式，要求所有导入必须使用 `.js` 扩展名。

### 修复方案

```typescript
// ✅ 正确：必须使用 .js 扩展名
import prisma from './prisma.js'
```

**为什么没有 .js 文件却能工作？**

这是 tsx 的运行时魔法！tsx 使用 esbuild 在运行时即时编译，看到 `.js` 扩展名时会自动查找同名的 `.ts` 文件并编译。

---

## 坑4：ts-node 不支持 ESM，需要使用 tsx

### 问题描述

使用 `ts-node` 运行 ESM 项目时会报错：

```
(node:1234) Warning: To load an ES module, set "type": "module" in the package.json or use the .mjs extension.
```

### 问题原因

`ts-node` 对 ESM 支持不完善，而 Prisma 7.x 需要 ESM 模式。

### 修复方案

```json
// package.json
{
  "scripts": {
    "db:test": "tsx src/db-test.ts",
    "dev": "tsx src/server.ts"
  }
}
```

安装 tsx：

```bash
npm install --save-dev tsx
```

---

## 坑5：外键约束错误

### 问题描述

创建文章时出现以下错误：

```
Foreign key constraint violated on the fields: (userId)
```

### 问题原因

创建文章时传入的 `userId` 在数据库中不存在，违反了外键约束。

### 修复方案

**必须先创建用户，再创建文章**：

```bash
# 1. 先创建用户
curl -X POST http://localhost:3000/api/user/create \
  -H "Content-Type: application/json" \
  -d '{"name":"测试用户","email":"test@123.com","password":"123456"}'
# 返回 userId

# 2. 使用返回的 userId 创建文章
curl -X POST http://localhost:3000/api/post/create \
  -H "Content-Type: application/json" \
  -d '{"title":"测试文章","content":"内容","userId":1}'
```

---

## 坑6：MySQL 自增 ID 不从 1 开始

### 问题描述

数据库表中的 ID 从 4、5、6 等数字开始，而不是从 1 开始。

### 问题原因

1. **MySQL 自增主键默认从 1 开始**，永远不会从 0 开始
2. **删除数据不会重置自增计数器**，之前运行的测试数据被删除后，计数器继续递增

### 重要结论

**这是完全正常的行为！** 自增 ID 的具体数值并不重要，重要的是：
- 每个记录有唯一的 ID
- ID 按顺序递增
- 外键关联能正确工作

### 如果非要从 1 开始（不建议）

```sql
-- 方法1: 重置自增计数器（需要先清空表）
ALTER TABLE User AUTO_INCREMENT = 1;
ALTER TABLE Post AUTO_INCREMENT = 1;

-- 方法2: 删除表重新创建
DROP TABLE User;
DROP TABLE Post;
-- 然后重新执行迁移
npx prisma migrate dev --name reset_tables
```

**不建议这样做**，因为生产环境中重置 ID 可能导致数据混乱。

---

## 坑7：package.json 依赖拼写错误

### 问题描述

```json
// ❌ 错误拼写
"dotencxv": "^17.4.2",

// ✅ 正确应为
"dotenv": "^17.4.2",
```

### 问题原因

依赖名称拼写错误，导致 `dotenv` 无法正确安装和加载环境变量。

### 修复方案

```bash
npm uninstall dotencxv
npm install --save-dev dotenv
```

---

## 🎯 总结

| 坑 | 核心原因 | 解决方案 |
|----|----------|----------|
| Prisma 7.x 语法变更 | 新旧语法不兼容 | 按新语法修改 schema.prisma |
| PrismaClient 导入 | 生成位置和实例化方式变更 | 从 output 目录导入，使用 adapter |
| ESM 扩展名 | Node.js ESM 规范要求 | 导入使用 .js 扩展名 |
| ts-node | 不支持 ESM | 使用 tsx |
| 外键约束 | userId 不存在 | 先创建用户再创建文章 |
| 自增 ID | 删除数据不重置计数器 | 正常行为，无需修复 |
| 依赖拼写 | dotencxv → dotenv | 修正依赖名称 |

---

## 📚 参考链接

- [Prisma 官方升级指南](https://docs.prisma.io/docs/v6/orm/more/upgrades/to-v7)
- [Prisma 7 迁移提示](https://www.prisma.io/docs/ai/prompts/prisma-7)
